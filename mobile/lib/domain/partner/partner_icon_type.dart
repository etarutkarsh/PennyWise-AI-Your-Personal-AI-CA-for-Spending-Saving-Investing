/// Financial product icon type — domain enum, no Flutter dependency.
/// Presentation layer resolves to IconData; keeps domain pure.
enum PartnerIconType {
  savings,
  trendingUp,
  gold,
  tax,
  autoSave,
  creditCard,
  generic,
}
